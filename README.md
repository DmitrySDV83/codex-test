# codex-test

Simple Node.js web application that responds with "Привет, мир!".

## Usage

Install dependencies (none external) and start the server:

```bash
npm start
```

The app listens on port 3000 by default.
